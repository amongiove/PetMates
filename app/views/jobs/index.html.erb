<div class="row d-flex justify-content-center mt-50 mb-50">
    <div class="col-md-6">
        <div class="card">
            <div class="card-body">
                <h1 class="form-title">Your Jobs</h1>
                <hr class="mb-30">
                <table class="table">
                    <thead>
                        <tr>
                        <th scope="col">Date</th>
                        <th scope="col">Sitter Acceptance</th>
                        <th scope="col">Job Status</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr>
                        <%@jobs.each do |job|%>
                        <td><%=link_to job.datetime, job_path(job)%></td>
                        <td><%=job.acceptance_status.capitalize%></td>
                        <td><%=job.completed?%></td>
                        <%end%>
                        </tr>
                    </tbody>
                    </table>
                <a class="text-white btn bg-blue btn-block shadow-sm" href="#">some button?</a>
            </div>
        </div>
    </div>  
</div>


