<!DOCTYPE html>
<html>
  <head>

    <title>PetMates</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    
  </head>

  <body>
<% if session[:user_id] %>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/"><img src="https://banner2.cleanpng.com/20180310/kde/kisspng-logo-dog-pet-cat-cute-cat-5aa41e0b992c40.6510594315207050356274.jpg" alt="" width="30" height="24" class="d-inline-block align-text-top">PetMates</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav mr-auto">
        <li class="nav-item">
          <a class="nav-link" href=<%=user_path(User.find_by(id: session[:user_id]))%>>Profile</a>
        </li>
        <%if User.find_by(id: session[:user_id]).onboarded? %>
          <%if User.find_by(id: session[:user_id]).type == "Owner"%>
            <li class="nav-item active">
              <a class="nav-link" href="#">Find Sitters <span class="sr-only"></span></a>
            </li>
          <%end%>
          <li class="nav-item active">
            <a class="nav-link" href="#">Jobs <span class="sr-only"></span></a>
          </li>
        <%end%>
      </ul>
    </div>
    <div class="collapse navbar-collapse justify-content-end" id="navbarCollapse">
      <ul class="navbar-nav ml-auto">
        <li class="nav-item">
          <a class="nav-link" data-method="delete" href=<%=logout_path%>>Log Out</a>
        </li>
      </ul> 
    </div>
  </nav>
<%end%>

    <%= yield %>

  </body>
</html>
